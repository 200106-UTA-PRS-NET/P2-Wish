@model SearchMovieGenreVM

@{
    ViewData["Title"] = "Search";
}

<h1>Search</h1>

<form>
    <select class="custom-select">
        @foreach (var item in Model.genres)
        {
            <option value=@item.GetHashCode()>@item</option>

        }

    </select>
</form>>